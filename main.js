

let a = prompt("Harif va sonlarni qo'shib yozing va javobini ko'ring");

let i = -1;

 
while(i < a.length -1){

  i++
  if(a[i] = Number(a[i])) {continue}

  document.write(String(a[i]));

  console.log(String(a[i]));
}
